<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secretaría CMD - Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link rel="stylesheet" href="../css/prestadoresPartials.css">
    <link rel="stylesheet" href="../css/sectionPrestadores/prestadoresHome.css">
</head>

<body>
    <header>
        <%- include('../partials/prestadoresHeader.ejs')%>
            <div>
                <nav class="secondHeader">
                    <a href="/prestadores/agregarMedico">Agregar Médico Público</a>
                </nav>
            </div>
    </header>
    <main>
        <br>
        <div class="encabezado">
            <h1>Listado de Médicos Públicos</h1>
            <div>
                <span>Los médicos públicos son aquellos que se muestran en la página para usuarios que <span style="font-weight: bold;">no</span> son trabajadores de los Consultorios Médicos David</span>
            </div>    
        </div>
        <div class="prestadorSection">
            
            <% for (let prestador of ps) {%>
                <div>
                    <section class="prestadorBox">
                        <img src="../../../img/profilepictures/<%= prestador.profileImg%>" alt="profileImg">
                        <div class="prestadorInfo">
                            <div class="prestadorNombre">
                                <% if(prestador.sexo == "masculino" && prestador.especialidad != ("Psicología" || "Nutrición" || "Psicopedagogía" || "Kinesiología")){ %>
                                    <span>Dr. </span><span style="text-transform: uppercase"><%= prestador.apellido %>,</span>
                                    <span><%= prestador.nombre %></span>
                                <% }else if (prestador.sexo == "femenino" && prestador.especialidad != ("Psicología" || "Nutrición" || "Psicopedagogía" || "Kinesiología")){ %>
                                    <span>Dra. </span><span style="text-transform: uppercase"><%= prestador.apellido %>,</span>
                                    <span><%= prestador.nombre %></span>
                                <% } else { %>
                                    <span>Lic. </span><span style="text-transform: uppercase"><%= prestador.apellido %>,</span>
                                    <span><%= prestador.nombre %></span>
                                <% } %>
                            </div>
                            <p><%= prestador.especialidad %></p>
                            <a href="/prestadores/editandoPrestador/<%= prestador.id %>"><button type="button" class="btn btn-primary">Editar</button></a>
                            <a href="/prestadores/home/confirmDelete/<%=prestador.id%>"><button type="button" style="text-decoration: none;" class="btn btn-danger">Eliminar</button></a>
                        </div>
                    </section>
                </div>
                <% } %>
        </div>
    </main>
</body>

</html>